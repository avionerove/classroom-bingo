# Render 원클릭 배포 설정 (Blueprint)
# https://render.com 에서 이 저장소를 연결하면 자동으로 무료 웹 서비스가 만들어집니다.
services:
  - type: web
    name: classroom-bingo
    runtime: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python server.py"
    autoDeploy: true
